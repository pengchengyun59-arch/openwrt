# This is an empty file, please overwrite it after generating the .config file with the source code
# ===================== 1. 基础架构（小米AX3600必选）=====================
CONFIG_TARGET_ipq807x=y
CONFIG_TARGET_ipq807x_generic=y
CONFIG_TARGET_ipq807x_generic_DEVICE_xiaomi_ax3600=y
CONFIG_TARGET_ROOTFS_SQUASHFS=y  # 适合路由器的只读文件系统，稳定性更高

# ===================== 2. MACVLAN虚拟网卡（单线多拨核心依赖）=====================
CONFIG_PACKAGE_kmod-macvlan=y  # MACVLAN内核模块，生成虚拟网卡的基础
CONFIG_PACKAGE_macvlan=y  # MACVLAN管理工具集
CONFIG_PACKAGE_luci-proto-macvlan=y  # LuCI图形化配置MACVLAN，无需命令行

# ===================== 3. 多拨与负载均衡（DHCP/单线多拨通用）=====================
CONFIG_PACKAGE_luci-app-mwan3=y  # 多拨核心插件，管理多线路负载均衡
CONFIG_PACKAGE_luci-app-mwan3helper=y  # 简化mwan3配置的图形化辅助工具
CONFIG_PACKAGE_dhcp-helper=y  # 增强DHCP协议兼容性，确保多线路IP稳定获取
CONFIG_PACKAGE_ip-full=y  # 提供ip命令，辅助虚拟网卡与多拨接口调试

# ===================== 4. OpenClash（科学上网）=====================
CONFIG_PACKAGE_luci-app-openclash=y  # OpenClash主插件
CONFIG_PACKAGE_luci-i18n-openclash-zh-cn=y  # OpenClash中文语言包
CONFIG_PACKAGE_coreutils=y
CONFIG_PACKAGE_coreutils-nohup=y  # OpenClash后台运行依赖
CONFIG_PACKAGE_wget=y  # OpenClash下载规则、内核依赖
CONFIG_PACKAGE_unzip=y  # OpenClash解压规则包依赖
CONFIG_PACKAGE_libustream-openssl=y  # OpenClash HTTPS请求依赖

# ===================== 5. Mentohust（校园网认证）=====================
CONFIG_PACKAGE_luci-app-mentohust=y  # Mentohust图形化配置插件
CONFIG_PACKAGE_mentohust=y  # Mentohust核心认证程序
CONFIG_PACKAGE_luci-i18n-mentohust-zh-cn=y  # Mentohust中文语言

# ===================== 6. 基础功能补充（确保易用性与稳定性）=====================
CONFIG_PACKAGE_luci=y  # LuCI Web管理界面
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y  # LuCI全局中文语言包
CONFIG_PACKAGE_sshd=y  # SSH远程管理（调试用，可选）
CONFIG_PACKAGE_htop=y  # 系统资源监控（查看CPU/内存占用，可选）
CONFIG_PACKAGE_luci-app-nlbwmon=y  # 流量统计插件（查看多拨带宽使用，可选）
CONFIG_PACKAGE_luci-app-speedtest=y  # 内置测速工具（验证多拨效果，可选）
